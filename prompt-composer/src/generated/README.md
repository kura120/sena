# Generated Proto Bindings

These files are generated by `tonic-build` from `daemon-bus/proto/sena.daemonbus.v1.proto`.

If `protoc` is not installed, the pre-committed copy here keeps the crate compilable.
To regenerate: install `protoc` and run `cargo build -p prompt-composer`.
