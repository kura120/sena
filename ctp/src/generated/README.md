# Generated gRPC code

This directory contains Rust code generated from the daemon-bus proto definitions by `tonic-build` in `build.rs`.

**Never edit these files manually.** They are overwritten on every build when `protoc` is available.

If `protoc` is not installed, a pre-committed placeholder file keeps the crate compilable during scaffolding.
